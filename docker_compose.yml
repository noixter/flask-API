version: "3.8"

services:
  db:
    image: postgres

  web:
    build: .
    environment:
      - SECRET_KEY=mJMTPdPcCOfw6QsYXCCJSaFtmGDWItR+k8m4fIxyClA=
      - DB_URL=db
    depends_on:
      - db
    ports:
      - "5000:5000"
    volumes:
      - .:/opt/flask_api